{
  "name": "encoding-converter-local-app",
  "version": "1.0.0",
  "private": true,
  "description": "Local-only app for experiencing character encoding differences.",
  "scripts": {
    "build:maps": "node tools/build-encoding-maps.js",
    "start": "node server.js"
  },
  "dependencies": {
    "iconv-lite": "^0.6.3"
  }
}
